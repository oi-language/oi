package main

func Work(value text) text {
    return value
}

func Build(values []text) Handles {
    var tasks Handles = derive [create one Work task handle for each {values} item]
    return tasks
}

type Handles []task[text]

func main(values []text) {
    Build(values)
}
